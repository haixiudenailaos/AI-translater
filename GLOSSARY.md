# 术语表使用说明

术语表用来告诉 AI"某个词应该怎么翻译"，确保人名、地名、专有名词等在整本书中翻译一致。

## 如何打开

主窗口菜单栏点击 **工具 → 术语库**。

## 如何添加术语

1. 在底部编辑区填写"原文术语"和"译文术语"
2. 选择一个分类（如角色名、技术术语等）
3. 点 **添加**

## 如何修改/删除

- **修改**：在列表中单击选中某条术语 → 修改内容 → 点 **更新**
- **删除**：在列表中单击选中 → 点 **删除** → 确认

## 搜索与筛选

- 顶部搜索框输入关键词，列表会实时过滤
- 分类下拉框可以按分类筛选，选"全部"显示所有

## 保存

点 **保存** 按钮才会真正生效。关闭窗口时如果还没保存，会提示你是否保存。

## 导入 / 导出

- **导出**：把当前术语表导出为文件，可用于备份或分享给其他人
- **导入**：把之前导出的文件导入进来，不会覆盖已有的术语，只会新增

> 导入导出仅支持 JSON 格式（`.json` 文件）。

## JSON 文件格式

如果你需要手动编辑或批量准备术语表，文件格式如下（`config/glossary.json`）：

```json
{
  "terms": [
    {"source": "原文", "target": "译文", "category": "分类"},
    {"source": "API", "target": "应用程序接口", "category": "技术"}
  ],
  "categories": ["通用", "技术", "专业"]
}
```

- `source`：原文中的词语
- `target`：对应的翻译
- `category`：分类名称，可以在三个默认分类之外自己加

## 没有术语表时

术语表为空不影响正常翻译，可以不用管它。
