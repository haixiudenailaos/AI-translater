# P1-5：运行时依赖，与 pyproject.toml [project.dependencies] 保持一致
# 测试依赖（pytest/pytest-cov）请使用：pip install -e ".[dev]"
httpx[http2]>=0.27.0
ebooklib>=0.18,<0.20
beautifulsoup4>=4.11.0
lxml>=4.9.0
Pillow>=9.0.0
CairoSVG>=2.7.0,<3.0
# chardet 7.x ships mypyc native pipeline extensions that crash with an
# access violation after extraction from a PyInstaller onefile bundle.
# Keep the last pure-Python release until upstream packaging is compatible.
chardet==5.2.0
requests>=2.28.0
aiohttp>=3.9.0
openai>=1.0.0
volcengine-python-sdk[ark]>=5.0.41,<6.0
pydantic>=2.5.0,<3.0
keyring>=24.0.0
